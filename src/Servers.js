
import React from 'react';
import Alert from 'react-bootstrap/Alert'
function Servers() {
    return (
        <Alert key={1} variant={'primary'}>
            In TF2, type 'connect prohunter69trades.com' into the console to join a wicked cool server!
        </Alert>
    );
}
export default Servers